<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BitCobblers.StockTrader.Views.DepositPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" />
        <ToolbarItem Text="Deposit" />
    </ContentPage.ToolbarItems>
    <ContentView>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Amount" FontSize="24" />
            <Entry Keyboard="Numeric" Text="{Binding Item.Text}" FontSize="Medium" />
            <Label Text="Comments" FontSize="24" />
            <Editor Text="{Binding Item.Description}" FontSize="Medium" Margin="0" />
        </StackLayout>
    </ContentView>
</ContentPage>